<div class="card text-dark bg-light mb-3 mx-auto" style="max-width: 40rem;">
  <h1 class="card-header">Editing Teacher</h1>
    <div class="card-body">
      <%= render 'form', teacher: @teacher %>

      <%= link_to 'Back', teachers_path, class: "mt-3 btn btn-secondary" %>
    </div>
  </div>
</div>